# VMSS Laws — Latent-Sweep Review Dispositions (Fix Pack B)

**RATIFIED 2026-07-21 under delegated orchestration (Jason → architect):** the gate, Fix Pack B as scoped, and the 61-instrument granularity are approved; the alternate ~21-act synthesis is closed as comparison material. Reserved to Jason personally, not delegated: doctrinal adjudication of the PART 3 contradiction flags, and the merge of the latent branch to main (the natural veto read of the 61 named entries).

Architect review, 2026-07-21, of `feat/vmss-laws-latent-corpus` @ f1daced (Phase M gate b025015, provenance correction 9a45fc5, independent verification f1daced). Companion data: `docs-review/vmss-laws-latent-mining/accounting-recheck.{mjs,json}` — the architect's mechanical re-derivation of the id accounting from the committed annex, replacing the never-committed `assign.mjs` result and the dangling "returned JSON's `excluded` array" reference at inventory:1571.

## Verdict

**The 61-instrument inventory is ACCEPTED as the Phase M gate**, and the independent verification record is accepted as its audit. The two-run convergence on every headline doctrinal call — home-rule exclusivity, the Central Banking Authority resolution of the spec's failed exemplar, the two parser blockers, the whitepaper anchor gap, the PCD/SAD flag-only class — is the strongest evidence available short of Jason's own reading, and the architect re-confirmed the parser blockers by direct regex inspection (check-canon.mjs:590 `codeEntries`; build-law-toc.mjs:126 `entryRe` + the :154 extraction-mismatch hard-fail).

Independently re-derived and **confirmed exactly**: the 506 assigned candidate ids (0 double-assignments; the two Path 2 promotions surface precisely as documented). Accepted on the verification record's own re-run: 0/61 name collisions, 0 reserved-phrase heads, clean name form.

**Not confirmed — and now closed differently than either run believed:** the accounting. Against the committed annex notes (the only committed evidence; the workflow journal's pool exists nowhere in-tree):

- **The pool is 1,352 ids, not 1,321 (journal) or 1,320 (inventory PART 0/PART 4).**
- The inventory's own Counts split never reconciled internally: 121 named + 640 residual ≠ 731 excluded.
- Coverage derived from the inventory: 505 assigned (+1 phantom citation) · 79 flag-cited · 100 named-excluded · 135 Path 2 categorical · **533 residual**, of which ~455 are self-excluding by mined class (charter-home, not-law, sims-restatement) and **26 are operative/escalation-class ids with no disposition anywhere** (`suspiciousResiduals` in the JSON), concentrated in `mine-wp-17-19.md` — the signature of a truncated miner return: the synthesis saw less of §17–19 than the committed notes contain.
- **2 phantom references:** instrument #30 cites `wp-17-19-18`, which does not exist — the annex defines `18a/18b/18c` (the provision text matches 18a); PART 2 cites `wp-17-19-79`, which the annex never defines.

None of this disturbs any of the 61 instruments' content; it means the *exclusion side* of the gate was unproven, exactly as the correction commit warned. Spot-reading the 26 shows most are duplicate-source ids of content the instruments already carry (e.g., `wp-17-19-35` mega-wall spec ↔ instrument #6; `-51` TIP ↔ #7; `-25` bailout location ↔ #28; `-41` analog redundancy ↔ #18) — but "most, probably" is not a closed ledger, and at least one genuine drop is confirmed: **`systems-66b`**, a real contradiction flag (systems.html adds a mutual-aid rung "escalating to +1 Sanctuary for maximum resources" that Charter XXV.IV does not contain) that never reached PART 3.

## Fix Pack B — Phase A preconditions (apply in order, each committed with evidence)

- **B1 — Rebase the accounting on the committed ledger.** `accounting-recheck.json` is ground truth until superseded; re-run the `.mjs` and commit the regenerated JSON after any inventory/annex edit. Amend the inventory's Counts block and PART 4 §5 to the derived numbers, and replace the inventory:1571–1572 reference to the lost workflow JSON with a pointer to this ledger. The stale claims are corrected in place per the 9a45fc5 idiom (original left standing where it documents provenance, correction beside it).
- **B2 — Disposition all 26 `suspiciousResiduals`.** Each id gets exactly one of: appended to a covering instrument's candidate-id line (where its content is already carried — expected for most), a named exclusion, or a PART 3 flag. **No new instrument and no provision-text change without architect sign-off**; any id whose content is genuinely uncarried escalates to Jason instead of being authored. Evidence: one line per id citing the covering provision or the exclusion ground.
- **B3 — Cure the two phantom citations.** #30's `wp-17-19-18` → `wp-17-19-18a`; then 18b (duress signaling — operative federal-floor telemetry rule) and 18c (hardware mortality remainder) are dispositioned under B2 on their own merits. PART 2's `wp-17-19-79 — LP-049` → verify the intended id against the annex's LP-049-related entry and cure or strike the line.
- **B4 — Restore the dropped contradiction flag `systems-66b`** to PART 3's contradictions list verbatim from the annex headline. Record, as an annex erratum note (do not renumber ids), the `mine-systems.md` headline-vs-table numbering skew around systems-65/66 (the headline calls Genetic Diversity Monitoring `systems-65`; the table and PART 3 have it at `systems-66`, with `systems-65` being the XXV.III charter-home row).
- **B5 — Ledger the remaining residuals.** The ~455 self-excluding residuals (CHX/NL/sims-restatement/unknown-class) each get their residual ground (a)/(b)/(c) recorded — mechanically, by extending the recheck tool's output into the residual block's promised per-id record. The residual block then states the derived count and points at the committed ledger.
- **B6 — Apply the two parser fixes + the whitepaper heading guard, mutation-tested.** Designs ratified as proposed in the inventory's architect notes (b) and (c): optional `data-instrument` capture in both regexes; *explicit* partition of founding entries out of guards (a1)/(a3)/(b)/(a4) rather than silent non-matching; `data-source` = bare page or resolving `file#anchor`, backed by a Whitepaper-`<h2>N.`-existence check. Mutation probes that must bite: a founding entry with `data-instrument` between `data-tier` and `data-source` must be seen by both parsers; a founding entry citing a nonexistent Whitepaper § must fail; a forged `data-instrument` vocabulary value must fail. The P4 lesson binds: a red build is evidence only if red for the reason under test.
- **B7 — Restate flag counts from the ledger.** PART 3 cites 79 flag-only ids plus 12 contradiction cross-cites of assigned ids; the "83 flag-owned" figure is restated from derived numbers alongside B1's corrections.
- **B8 — §9.3 schema backfill.** Every one of the 61 instruments gains the v1.1 addendum's machine-readable fields — `parent-authority` (already present on the 10 schedule-under-authority instruments; founding acts record their licensing article or `none-standalone`) and `charter-touchpoints` (every Charter article cited/restated/depended on, each marked *derives-authority* vs *restates-content*, with file:line). This is Phase A authoring input and feeds the load-bearing audit (`vmss-laws-loadbearing-handoff.md` §3).
- **B9 — Spec sync before executing.** Merge `origin/claude/laws-html-visibility-5nppos` into the latent branch first: it carries the latent handoff v1.1 (§9 addendum), the load-bearing successor spec, and this fix pack + ledger tooling. Neither prior run had the v1.1 spec; Phase A must.

## Ratifications (no action required)

- **J-B1 — The Central Banking Authority home-rule call**: endorsed. The sweep correctly followed the rule over the spec's illustrative example; the departure is documented at the right altitude (architect note (a)).
- **J-B2 — Deferring Phase A at the gate**: endorsed — handoff §7's resume clause exists for exactly this; authoring onto an unproven exclusion ledger would have been the wrong risk.
- **J-B3 — The alternate ~21-act consolidation** (the second run's out-of-tree synthesis): the 61-instrument granularity stands per the verification's own analysis (canon supplies distinct proper names rule 2 forbids merging). The alternate is comparison material for Jason on request, not a pending decision.

## Launch block (paste into the next Opus session, repo root)

```
You are completing the VMSS latent-corpus sweep: Fix Pack B, then Phase A and Phase V. Use ultracode for fan-out; every rule binds subagents.

Setup: verify `git remote -v` → JasonHuang24/VMSS. Check out feat/vmss-laws-latent-corpus (origin tip), then merge origin/claude/laws-html-visibility-5nppos into it (spec sync — Fix Pack B9). Read fully, in order: docs-review/vmss-laws-architecture.md · docs-review/vmss-laws-latent-handoff.md v1.1 including §9 · docs-review/vmss-laws-latent-dispositions.md (Fix Pack B — this run's first job) · docs-review/vmss-laws-latent-inventory.md · docs-review/vmss-laws-latent-verification.md · docs-review/vmss-laws-opus-prompts.md Prompt 0.

Execute in order: (1) Fix Pack B1–B7 — close the accounting against docs-review/vmss-laws-latent-mining/accounting-recheck.json, disposition the 26 suspiciousResiduals, cure the phantoms, restore systems-66b, apply and mutation-test the parser fixes; commit with evidence, re-run the recheck tool, commit the regenerated JSON. (2) Phase A per handoff §7 with the B8 schema backfill and v22.8.0 versioning (§9.2) — laws.html founding entries, register-intro cure, R23 registration, guard extensions. (3) Phase V adversarial verification. Gates: check-canon 0 failures (report count vs the baseline you measure), build:css clean, both TOC modes idempotent, recheck JSON regenerated and clean of suspiciousResiduals.

Reporting to docs-review/vmss-laws-latent-worklog.md per phase; judgments beyond spec flagged; commit messages end "Co-Authored-By: Claude Opus <noreply@anthropic.com>". Stop conditions per handoff §8: any unresolvable gate, any temptation to weaken a check, any unavoidable doctrine authorship → stop at last green state and record. Push feat/vmss-laws-latent-corpus when done (git push -u origin feat/vmss-laws-latent-corpus). No PR, no merge, never main.
```
