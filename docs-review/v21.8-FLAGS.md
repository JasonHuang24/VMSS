# v21.8 — FLAGS

Judgment calls and substance-boundary notes for the LP-062.3 entry. The SOURCE
SUBSTANCE block + RULINGS + existing canon supported the full entry; **no fact in
the draft required invention beyond them.** Items below are recorded decisions,
not gaps, except where noted.

1. **RULING A — path taken: PRIMARY, not fallback.** The entry uses the existing
   `status-failed` visual class with the distinct banner **"Rejected ·
   Constitutional Conflict (dual-key review)"**. The registry's filter / stat /
   vote-semantics machinery keys on the `status-failed` *class* (and
   `data-status`), never on banner text, so a third failed-family *label* is
   hosted cleanly — exactly the precedent LP-062 sets (same class, custom banner
   "Failed at Ratification · Dual-Key Challenge Denied"). No fallback to a
   generic "Failed" badge was needed; the gate is named in the entry's first
   line regardless.
   - Separator rendered as `&middot;` (badge house style) rather than the em-dash
     the ruling text used, matching the sibling badge.
   - There is **no per-status stat card or numeric status counter** on the page
     (the filter chips are unlabeled by count). So RULING A's "status-family
     counts" concern has no surface to update: only the total (82 → 83) and the
     Regulatory tier (21 → 22) changed.

2. **§5.6 public-signal component** is cited as LP-062.2's daylight mechanism.
   Not invented — the existing §15.4 engraving already names "STI's public-signal
   component (§5.6)", and the LP-062.2 entry describes the same public-rating
   component. The entry reuses that anchor.

3. **Vote block has no ratification row** — correct: the filing died at the
   classification gate and never reached a ratification vote (RULING A / RULING
   B same-year kill). The two classification rows carry `td.fail` (satisfying the
   check-canon invariant that a `status-failed` entry shows ≥1 failing gate); the
   Independent Cold Review row is a neutral `td` (the cold review is corroborating
   evidence, not a gate).

4. **Title** — "Third Pass (Actionable Opt-Out Signal)" mirrors the sibling
   naming convention ("Second Pass (Opt-Out-Able Default Redraft)"). Authored to
   fit the pattern; not from the substance block verbatim.

5. **Placement / tier** — LP-062.3 sits in the Regulatory Petitions section with
   its siblings (regulatory-filed, Article XXVIII, Main Layer) and is counted
   regulatory. The build script groups by document position, so the entry counts
   in the tier its filing belonged to even though it was rejected at
   classification.

## Rider disposition
- The OPTIONAL RIDER (§5.4.2 LP-026 cadence) was **enacted** — the founder left
  the block in the brief. Tracked previously in the Wave-2 audit-table docket
  (item 2) as available-if-desired; now resolved.
