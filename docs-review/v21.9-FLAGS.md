# v21.9 — Economic Doctrine Amendments — FLAGS + OPEN DOCKET + WORKLOG

Branch: `v21.9-econ-amendments` (never main). Doubles as this pass's worklog: the
handoff's Expected surface authorizes an "open-docket file" but not a separate
worklog, so worklog content lives here rather than in an unlisted `v21.9-worklog.md`
(see FLAG 9). Every judgment call is one item below.

## Changes per file (worklog)

### whitepaper.html
- **§12.5** — three paragraphs appended after the LP-069 attribution paragraph:
  - *Task 1* idle-commercial enforcement: enumerated founder-calibratable schedule of
    attribution-vulnerable settlements (window-reset / tenant-occupancy / origination
    valuation), cleared-settlement-as-representation, simulated-settlement-as-evasion
    under §12.6 system-sabotage, clearing-record pattern scan (no human review), annual /
    multi-cycle-confidence adjudication, liability retroactive to first simulated reset.
    Sole-sensor / no-registry invariant restated. Cites (LP-070).
  - *Task 4* pulse-ordering: 90-day rolling average read at month start; in-month deposits
    → next window; extends III.VII pulse-at-start to the district trigger. [FOUNDER-EDITABLE]
  - *Task 3* trigger-geography accepted tension: district-scoped trigger relocation seam
    ruled a do-nothing on relocation friction; §28.0 named-residual; tripwire = inter-district
    duty-cycle divergence correlated with out-migration.
- **§12.6** — treasury paragraph inserted before the economic-flow callout (RULING-POCKET,
  mirrors §12.3 "infrastructure, not policy"); economic-flow callout rewritten to route
  automation→ADT and taxation→Main treasury→surplus→ADT, SCM→ADT. Coverage-ratio rate-gate
  carries a `[FOUNDER-SET]` placeholder. Cites (LP-070).

### charter.html (III.III only)
- *Task 2a* one sentence added: top-marginal revenue funds the Main Layer treasury
  (benefit-ratio doctrine; no discretionary surplus; remainder → ADT; Whitepaper §12.6).
- *Task 2b* restatement marker added after the rate `<ul>` (**RULING-TIER**). Rate values
  **untouched**. Marker convention defined once, reusably (FLAG 5).

### law-polling.html
- **LP-070** entry inserted at the tail of the Federal section (status-enacted). In-world
  margins/dates/drafter = `[FOUNDER-AUTHOR]` (FLAG 7). Stat cards 83→84 / Federal 54→55;
  filter-count 83→84; TOC regenerated by `build-law-toc.mjs` (84; 7/55/22).

### docs-review/
- `lp-audit-table.md` — LP-070 row (ENGRAVED v21.9, born); Follow-up-docket item 3 (Charter
  restatement audit); v21.9 checks block.
- `RATIFY-TAX-20-petition-draft.md` — *Task 6* gated federal petition draft (20/10/5/2.5).
  Unmerged, NOT in the registry, touches no rate table.
- `v21.9-FLAGS.md` — this file.

## Gate compliance
- **RATIFY-TAX-20 UNSAID — honored.** `70 / 35 / 17 / 8` untouched in whitepaper §12.1,
  charter III.III, and everywhere else. `grep` confirms no rate-value edit. The cascade
  20/10/5/2.5 exists only inside the gated draft.
- **No SCM parameter touched.** Triggers, rates, scopes, windows all stand as engraved.

## Flags (judgment calls)
1. **Version stamp NOT bumped — STOP boundary.** Expected surface excludes README.md /
   footer.html and says "Anything outside: STOP." Convention (RULING E, v21.8) bumps both on
   a version. Left both at **21.8**; check-canon's version-lockstep guard passes because
   README==footer. **DECISION NEEDED:** if v21.9 should carry a footer/README stamp, it's a
   one-line bump on each — flagged, not done.
2. **RULING-TIER binding source VERIFIED.** Whitepaper §12.1 (lines 866–872) genuinely holds
   the `Top Marginal Rate` table, so the III.III marker restates a real source, not a dangling
   pointer. Marker names §12.1 textually + links the live `#article-xxv-vi` anchor. I did **not**
   invent a "federal register LP number" for the base rates — none exists; they are original
   doctrine at federal tier. The marker states the tier + route without fabricating a citation.
3. **§12.6 flow reconciliation (automation vs. tax → treasury).** RULING-POCKET says
   *top-marginal tax* funds the Main treasury; Task 2's gloss said *automation and taxation*.
   Existing canon (§12 intro, ADT glossary) has automation funding the **ADT** directly. I
   reconciled to preserve that: **automation-side output → ADT; taxation → Main treasury;
   treasury surplus → ADT; SCM → ADT** — honoring RULING-POCKET and the coverage-ratio gate
   ("automation-side revenue" as the coverage *measure*) without rerouting automation through
   the treasury and contradicting the ADT's definition. **If you intend automation revenue to
   flow through the Main treasury first, the flow line + treasury paragraph need a tweak.**
4. **Restatement marker convention (defined once).** class `charter-restatement`; format =
   italic "Restatement." lead → binding source (§12.1) + tier (federal-law) → recalibration
   route (Article XXV.VI, live link) → precedence rule ("binding source controls; the figures
   are not amended by editing this article"). **No new CSS** — uses existing utility classes,
   so it stays strictly within III.III. Reusable for the queued full audit.
5. **Open-docket location = lp-audit-table.md Follow-up docket.** That section already holds the
   repo's deferrals (LP-039, LP-026), so the Charter restatement audit logs there as item 3
   rather than in a new file.
6. **LP-070 = Enacted / Federal**, covering Tasks 1–4, born-engraved at §12.5/§12.6. Dual-key
   rationale follows the LP-069 precedent; RULING-TIER makes the rate tier federal, supporting
   federal classification (not Article XI). Flag if you read the treasury architecture as
   structural enough to route to Charter instead.
7. **LP-070 in-world metadata = `[FOUNDER-AUTHOR]` placeholders** (margins, dates, drafter), per
   Task 5. Vote rows keep `class="pass"` so the status-enacted / no-failing-gate canon-check
   invariant holds; the Threshold row uses the fixed Article XXV.VI structural thresholds (known,
   not a placeholder). The v21.6 precedent instead authored plausible margins — say which
   convention you want and I'll switch.
8. **Founder knobs left open:** treasury coverage ratio `[FOUNDER-SET]` (§12.6 + petition);
   attribution-vulnerable settlement schedule (founder-calibratable); trigger-geography tripwire;
   Task-4 pulse line (founder-editable); ~$2B ceiling in the petition (order-of-magnitude).
9. **No separate worklog file** — folded here to respect the "STOP outside the surface" rule
   (the surface lists an open-docket file, lp-audit-table.md, and the LP draft; not a worklog).
   Say the word if the per-pass `vXX-worklog.md` convention should be restored as its own file.

## Open docket (carried forward)
- **Charter restatement audit** (also logged at `lp-audit-table.md` → Follow-up docket #3):
  III.III is the first restatement marker; a systematic sweep of every Charter article to mark
  binding-vs-restatement text is queued as its own workstream. Rationale: any misplaced
  restatement reads as amendment-tier protection over what is actually federal law — cheap to
  mark now, expensive to audit later.

## Checks (local)
- `node tools/check-canon.mjs` → **68 passed, 0 failed** (exit 0); derived `law 7/55/22 of 84`.
- `node tools/build-law-toc.mjs` → **84 entries** (Charter 7 · Federal 55 · Regulatory 22), exit 0.
- CI (GitHub Actions) not yet run at time of writing — see final push confirmation.
